---
"@open-slide/core": patch
---

Design panel saves prefer `mergeDesign` exports, drop a duplicate legacy `const design`, and parse the merged patch so token edits do not reintroduce a second binding.
